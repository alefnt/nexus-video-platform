# Git 统计报告

- 时间：2025-10-21 21:28:52 +08:00
- 项目路径：D:\\new_sp\\video-platform

## 仓库状态
- 当前目录不是 Git 仓库，无法获取提交历史与作者统计。
- 已记录一次“工作区快照统计”，用于在未启用 Git 的情况下进行基线记录。

## 工作区文件类型统计（排除构建与vendor目录）
- 排除：`node_modules`、`dist`、`build`、`.git`、`test-results`、`client-web/public`、`contracts`、`client-mobile/lib`
- 统计结果：
  - `.ts`: 29
  - `.json`: 20
  - `.tsx`: 16
  - 无扩展名: 5
  - `.md`: 4
  - `.css`: 1
  - `.log`: 1
  - `.development`: 1
  - `.html`: 1
  - `.bak`: 1

## 建议的后续动作（启用 Git 管理）
1. 初始化 Git 仓库并创建基线提交：
   - `git init`
   - 创建 `.gitignore`（建议忽略：`node_modules/`, `dist/`, `build/`, `test-results/`, `*.log`, `*.bak`）
   - `git add .`
   - `git commit -m "chore: baseline commit (init repo)"`
2. 生成作者与提交统计（启用后可执行）：
   - 总提交数：`git rev-list --count HEAD`
   - 作者贡献：`git shortlog -s -n --all`
   - 最近30天提交：`git log --since=30.days --pretty=format:"%h %an %ad %s" --date=short`
3. 将统计记录到 `docs/` 下，按时间戳命名，形成持续的历史档案。

---
此报告由一次“Git统计请求”生成；因仓库尚未初始化，已以文件快照作为替代基线，供后续对比与追踪。